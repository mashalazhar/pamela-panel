<%= form_for @course do |f| %>  
    <ol><%= f.label :name %></ol>
    <ol><%= f.text_field :name, value: @course.name %></ol> 
    <ol><%= f.label :total_hours %></ol>
    <ol><%= f.number_field :total_hours, value: @course.total_hours %></ol>
    <ol><%= f.submit %>
<% end %>

