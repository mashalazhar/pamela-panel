<%= form_for @course do |f| %>  
    <%= f.label :name %>  
    <%= f.text_field :name, value: @course.name %>  
    <%= f.label :total_hours %>  
    <%= f.number_field :start_date, value: @course.total_hours %>
    <%= f.submit %>
<% end %>


<%= link_to 'Delete', course_path(@course), method: :delete %> 